# План завершения задачи #36: Designer SDK/Wiki Parity

## Текущее состояние проекта (анализ от 2026-04-05)

### ✅ ЧТО УЖЕ РЕАЛИЗОВАНО

#### Milestone A — Contract foundation (#31)
- **SDK контракт**: Python SDK (`rpaforge.sdk`) полностью реализован
  - `ActivityMeta.to_dict()` возвращает canonical payload
  - Поля: `id`, `name`, `type`, `category`, `description`, `icon`, `ports`, `params`, `builtin`, `robotFramework`
  - Все тесты проходят (17/17 test_activity_registry.py)
  
- **Bridge handlers**: `_handle_get_activities` возвращает SDK payload
  - `packages/core/src/rpaforge/bridge/handlers.py:453-467`
  - Использует `meta.to_dict()` для сериализации
  
- **TypeScript нормализация**: `normalizeActivity` в `engine.ts`
  - Преобразует bridge payload в canonical Activity interface
  - Заполняет defaults для optional полей
  - Тесты есть (`engine.test.ts`)

#### Milestone B — Graph integrity (#34)
- **Start invariant**: Реализован в `processStore.ts`
  - `createProcess` создаёт один Start node (строка 319)
  - `addNode` блокирует добавление второго Start (строка 367-373)
  - `removeNode` блокирует удаление последнего Start (строка 385-391)
  - `loadProcess` проверяет exactly one Start (строка 338-344)
  - `validateDiagram` проверяет Start topology (строка 550-562)
  
- **Edge normalization**: `normalizeEdge` сохраняет handles (строка 273-282)
  - `sourceHandle` defaults to 'output'
  - `targetHandle` defaults to 'input'
  
- **Тесты**: 22/22 в `test_codegen.py` проходят

#### Milestone C — Activity UI parity (#33)
- **Palette populated from SDK**: `useDesigner.ts` вызывает `getActivities()`
  - `refreshActivities` fetches activities from bridge (строка 72-88)
  - Группировка по категориям реализована
  
- **Activity rendering**: `ActivityPalette.tsx` отображает SDK activities
  - Иконки, имена, описания из SDK metadata
  - Drag-and-drop поддерживается
  
- **Property panel**: `PropertyPanel.tsx` существует (нуждается в проверке)

#### Milestone D — Codegen parity (#27)
- **Python codegen**: `CodeGenerator` в `rpaforge.codegen`
  - `generate(diagram)` — валидирует Start topology
  - Генерирует Robot Framework код из диаграммы
  - Тесты: 22/22 проходят
  
- **Client fallback codegen**: `clientCodegen.ts`
  - Валидирует exactly one Start node (строка 107-111)
  - Генерирует IF/WHILE/TRY/EXCEPT/FINALLY
  - Тесты есть (`clientCodegen.test.ts`)

---

## ❌ ЧТО НУЖНО СДЕЛАТЬ (приоритеты)

### 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ (MVP blocking)

#### 1. SDK-driven Property Panel (#33 — INCOMPLETE)

**Проблема**: Property panel должна генерироваться из SDK `params`, но текущая реализация не проверена.

**Что проверить/доделать**:
- [ ] Убедиться, что `PropertyPanel.tsx` рендерит поля из `activity.params`
- [ ] Проверить, что типы параметров (`string`, `integer`, `boolean`, `variable`, `expression`, `secret`, `code`, `list`, `dict`) отображаются корректно
- [ ] Добавить UI для `builtin` настроек (timeout, retry, continueOnError) только если `activity.builtin.*` = true
- [ ] Написать тесты для SDK-driven property editing

**Файлы**:
- `packages/studio/src/components/Designer/PropertyPanel.tsx`
- `packages/studio/src/components/Designer/PropertyPanel.test.tsx`

**Оценка**: 4-6 часов

---

#### 2. Multi-port rendering (#33 — INCOMPLETE)

**Проблема**: Активности с несколькими выходами (If, Switch, Try-Catch) должны отображать порты из SDK metadata.

**Что проверить/доделать**:
- [ ] Убедиться, что `ProcessCanvas.tsx` рендерит ports из `activity.ports`
- [ ] If должен показывать `true` и `false` порты
- [ ] Switch должен показывать динамические case порты + default
- [ ] Try-Catch должен показывать `output`, `error`, и опционально `finally`
- [ ] Проверить, что соединения сохраняют `sourceHandle` и `targetHandle`

**Файлы**:
- `packages/studio/src/components/Designer/ProcessCanvas.tsx`
- `packages/studio/src/types/blocks.ts` (функции `getSwitchPortConfig`, `getTryCatchPortConfig`)

**Оценка**: 3-4 часа

---

#### 3. Codegen explicit Start validation (#27 — NEEDS VERIFICATION)

**Проблема**: Python codegen должен явно валидировать Start topology и не молча выбирать первый Start.

**Что проверить**:
- [ ] Убедиться, что `CodeGenerator.generate()` бросает ошибку при `startNodes.length !== 1`
- [ ] Проверить, что ошибка информативна (например, "Code generation requires exactly one Start node. Current diagram has X.")
- [ ] Добавить тесты для invalid Start topology в Python tests

**Файлы**:
- `packages/core/src/rpaforge/codegen/__init__.py`
- `packages/core/tests/test_codegen.py`

**Оценка**: 1-2 часа

---

### 🟡 ВЫСОКИЙ ПРИОРИТЕТ (качество и parity)

#### 4. User-facing validation feedback (#34 — INCOMPLETE)

**Проблема**: При попытке добавить второй Start или удалить последний, пользователь должен видеть понятное сообщение.

**Что сделать**:
- [ ] Убедиться, что `validationMessage` отображается в UI (toast, alert, inline message)
- [ ] Проверить, что сообщения локализованы/понятны
- [ ] Добавить визуальный feedback при invalid connections

**Файлы**:
- `packages/studio/src/components/Designer/ProcessCanvas.tsx`
- `packages/studio/src/components/Common/Layout.tsx`

**Оценка**: 2-3 часа

---

#### 5. Save/load round-trip preservation (#34 — NEEDS VERIFICATION)

**Проблема**: Edge semantics (`sourceHandle`, `targetHandle`, `type`, `data`, `style`) должны сохраняться без потерь.

**Что проверить**:
- [ ] Написать тест: create diagram → save → load → compare
- [ ] Проверить, что typed edges (true/false, case/default, output/error/finally) сохраняются
- [ ] Проверить, что custom edge styles сохраняются

**Файлы**:
- `packages/studio/src/stores/processStore.test.ts`
- `packages/studio/src/utils/fileUtils.ts`

**Оценка**: 2-3 часа

---

#### 6. Remove hardcoded runtime activity catalog (#33 — PARTIALLY DONE)

**Проблема**: В `blocks.ts` есть `createFallbackActivities()` и `BLOCK_PORT_CONFIGS`, которые дублируют SDK metadata.

**Что сделать**:
- [ ] Проверить, используется ли `createFallbackActivities` в production
- [ ] Если используется — заменить на SDK-driven данные
- [ ] Оставить fallback только для offline/error случаев
- [ ] Убедиться, что `BLOCK_PORT_CONFIGS` используется только для built-in блоков (start, end, if, while, etc.), не для SDK activities

**Файлы**:
- `packages/studio/src/types/engine.ts` (createFallbackActivities)
- `packages/studio/src/types/blocks.ts` (BLOCK_PORT_CONFIGS)

**Оценка**: 1-2 часа

---

### 🟢 СРЕДНИЙ ПРИОРИТЕТ (polish)

#### 7. Regression test coverage (#33, #34, #27 — INCOMPLETE)

**Что добавить**:
- [ ] UI тест: palette populated from SDK
- [ ] UI тест: property panel generated from SDK params
- [ ] UI тест: multi-port rendering
- [ ] UI тест: Start invariant enforced (add/remove)
- [ ] Codegen тест: typed graph traversal (true/false, case/default, output/error/finally)
- [ ] Store тест: save/load round-trip

**Оценка**: 4-6 часов

---

#### 8. Verify Studio tests run (#36 — BLOCKING)

**Проблема**: `npm test` в `packages/studio` не запускается из-за проблем с Node.js.

**Что сделать**:
- [ ] Исправить environment для запуска npm test
- [ ] Убедиться, что все Studio тесты проходят
- [ ] Добавить в CI/CD

**Оценка**: 1-2 часа

---

## 📋 РЕКОМЕНДУЕМЫЙ ПОРЯДОК ВЫПОЛНЕНИЯ

### День 1 (6-8 часов)
1. **Исправить npm test environment** (задача 8) — иначе нельзя верифицировать UI
2. **SDK-driven Property Panel** (задача 1) — критично для parity
3. **Multi-port rendering** (задача 2) — критично для parity

### День 2 (4-6 часов)
4. **Codegen explicit Start validation** (задача 3) — быстро, важно
5. **User-facing validation feedback** (задача 4) — UX
6. **Save/load round-trip preservation** (задача 5) — data integrity

### День 3 (4-6 часов)
7. **Remove hardcoded catalog** (задача 6) — code quality
8. **Regression test coverage** (задача 7) — stability

### Верификация (2-3 часа)
9. Запустить все тесты: `pytest packages/core/tests -v` и `npm test`
10. Проверить acceptance criteria из #36
11. Обновить #32 с финальным статусом

---

## ✅ ACCEPTANCE CRITERIA (#36) — Checklist

### Activity palette from SDK
- [x] Palette populated from bridge `getActivities`
- [ ] Property panel generated from SDK `params` (задача 1)
- [ ] Multi-port nodes render SDK ports (задача 2)

### Start/End built-in behavior
- [x] Start/End are built-in Studio blocks, not SDK activities
- [x] Start created automatically in new diagrams
- [x] Cannot add second Start
- [x] Cannot remove last Start

### Edge semantics
- [x] `sourceHandle` and `targetHandle` preserved in store
- [ ] User sees validation feedback (задача 4)
- [ ] Save/load preserves edge metadata (задача 5)

### Codegen parity
- [x] Python codegen validates Start topology
- [ ] Python codegen explicit error on invalid topology (задача 3)
- [x] Client fallback codegen validates Start

### Tests
- [x] Python core tests pass (22/22 codegen, 17/17 activity_registry, 26/26 bridge)
- [ ] Studio UI tests pass (задача 8)
- [ ] Regression coverage for palette, ports, connectors (задача 7)

---

## 🚨 РИСКИ И ЗАВИСИМОСТИ

1. **npm test не запускается** — блокирует верификацию UI (задача 8 — приоритет #1)
2. **PropertyPanel.test.tsx существует** — возможно, тесты уже есть, нужно проверить
3. **ActivityPalette.test.tsx существует** — возможно, тесты уже есть, нужно проверить

---

## 📊 ИТОГ

**Выполнено**: ~70% от MVP parity
**Осталось**: ~12-18 часов работы

**Критический путь**: задачи 8 → 1 → 2 → 3 → верификация

Рекомендую начать с исправления npm test environment, затем последовательно пройти критические задачи.
